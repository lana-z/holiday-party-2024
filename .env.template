# Template for environment variables
# Copy this file to .env.local and fill in your values

# Guest list as a JSON string (server-side only)
GUEST_LIST={}

# Party address (server-side only, replace with actual address)
PARTY_ADDRESS="Your Address Here"
